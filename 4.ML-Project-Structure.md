A colleague has initiated a new machine learning project located at /root/code/fraud-detection/. However, the current layout does not adhere to the xFusionCorp Industries standards. Your task is to restructure the project to align with the team's conventions.

Inspect the existing project at /root/code/fraud-detection/.

The final layout must match the tree below exactly:

fraud-detection/
├── data/
│   ├── raw/
│   └── processed/
├── models/
├── notebooks/
├── src/
│   ├── data/
│   ├── features/
│   ├── models/
│   └── utils/
├── tests/
├── configs/
├── requirements.txt
└── README.md

Every subdirectory under src/ must contain an __init__.py file so that Python recognises it as a package.

The project includes a requirements.txt listing its dependencies — scikit-learn, pandas, numpy, and mlflow.

README.md must begin with the heading # fraud-detection.

Review the existing project and correct everything that does not match the requirements above.
